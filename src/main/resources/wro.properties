resourceWatcherUpdatePeriod=1
ignoreMissingResources=false
cacheUpdatePeriod=10
modelUpdatePeriod=0
debug=true
#uriLocators=servletContext,uri,classpath

# List of processors:
# http://wro4j.readthedocs.io/en/stable/AvailableProcessors/

# TODO: use minified assets and add removeSourceMapsfrom 
# https://github.com/michael-simons/wro4j-spring-boot-starter
# => prevents harmless but annoying error in the logs  

preProcessors=lessCssImport,semicolonAppender   
postProcessors=less4j
#postProcessors=lessCss


# in prod, remove the updaters, set debug=false and add 
# jsMin,cssMinJawr to post processors