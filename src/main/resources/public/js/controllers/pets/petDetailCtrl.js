'use strict';

angular.module(window.GLOBAL.appName)

/**
 * Pet detail controller
 */
.controller('PetDetailCtrl', function($scope, $location, $window, $http, $routeParams) {
	console.log("inside pet detail controller: " + $routeParams.id);
	
	//toastr.success("inside admin controller");
});











